---
name: "YAML Lint"

on:
  pull_request:
    branches: ["main"]
    paths:
      - "kubernetes/**"
      - "talos/**"

concurrency:
  group: ${{ github.workflow }}-${{ github.event.number || github.ref }}
  cancel-in-progress: true

jobs:
  yaml-lint:
    name: YAML Lint
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Lint YAML files
        uses: ibiqlik/action-yamllint@2576378a8e339169c23c544b2a32e2c7a68e266d # v3.1.1
        with:
          config_file: .github/yamllint.yaml
          file_or_dir: kubernetes/ talos/
          strict: false
